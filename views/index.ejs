<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <!-- Import Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@700&display=swap" rel="stylesheet">    
    <style>
        body {
            background: linear-gradient(120deg, #3a7bd5 100%, #3a6073 100%);
            font-family: 'Roboto Slab', serif;
            margin: 0;
            padding: 0;
        }

        .container {
            margin-top: 80px;
            margin-left: 50px;
            width: 90%;
            padding: 50px;
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .left-side {
            flex: 1;
            text-align: center;
        }

        /* Rotating effect for Performance 360 */
        .left-side img {
            width: 100%;
            max-width: 600px;
            border-radius: 12px;
            /* Apply rotation */
        }

    
        .right-side {
            flex: 1;
            text-align: center;
        }

        /* Unique font for Welcome and Performance 360 */
        h1, .performance-title {
            font-family: 'Montserrat', sans-serif;
            color: #f2f2f2;
        }

        h1 {
            font-size: 48px;
            margin-bottom: 40px;
            font-family: 'Poppins', sans-serif;
        }

        .performance-title {
            font-size: 36px;
            margin-bottom: 20px;
        }

        .login-group, .register-group {
            margin-bottom: 40px;
        }

        .login-group h2, .register-group h2 {
            font-size: 28px;
            margin-bottom: 20px;
            color: #f2f2f2;
        }

        .btn-group {
            display: flex;
            justify-content: center;
            gap: 30px;
        }

        .btn-group button {
            background-color: #3498db;
            color: white;
            border: none;
            padding: 14px 25px;
            border-radius: 8px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease;
            margin: 5px 0;
            width: 100%;
            max-width: 280px;
        }

        .btn-group button:hover {
            background-color: #2980b9;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                text-align: center;
            }

            .left-side img {
                max-width: 100%;
            }

            .btn-group {
                flex-direction: column;
                width: 100%;
                gap: 10px;
            }
            
        }
        .highlight {
            display: inline-block;
            color: #3498db;
            font-size: 42px;
            font-family: 'Poppins', sans-serif;
            position: relative;
            transition: color 0.4s ease-in-out;
        }

        /* Smooth underline on hover */
        .highlight::after {
            content: '';
            position: absolute;
            width: 0;
            height: 3px;
            background-color: #e74c3c;
            left: 0;
            bottom: -5px;
            transition: width 0.4s ease-in-out;
        }

        .highlight:hover::after {
            width: 100%;
        }

        /* Color change on hover */
        .highlight:hover {
            color: #e74c3c;
        }

        p {
            font-size: 18px;
            margin-top: 20px;
        }


       
       
    </style>
</head>
<body>
    <div class="container">
        <div class="left-side">
            <!-- Rotating Image -->
            <img src="/images/logo.jpg" alt="Appraisal Logo">
        
        </div>
        <div class="right-side">
            <h1>Welcome to <span class="highlight">Performance 360</span></h1>
        <p>Explore dynamic performance management with a seamless experience.</p>
    
            
            <div class="login-group">
                <h2>Login</h2>
                <div class="btn-group">
                    <button onclick="window.location.href='/principal/login'">Principal Login</button>
                    <button onclick="window.location.href='/faculty/login'">Faculty Login</button>
                </div>
            </div>

            <div class="register-group">
                <h2>Register</h2>
                <div class="btn-group">
                    <button onclick="window.location.href='/principal/register'">Principal Register</button>
                    <button onclick="window.location.href='/faculty/register'">Faculty Register</button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
